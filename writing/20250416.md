# 20250416

- 最近 V してるんだけど、その中で `I've been V-ing lately, and ,,,`
  - I've been working on device integration lately, and I happened to notice some unexpected behavior.
- also は便利やで
- V するための〇〇、と言いたいとき `〇〇 that allows you to V` か `〇〇 lets you V`
- not はより前に持ってくる

> [ChatGPT]
> I've been tinkering `with` the Hear Better App, a sample app released by Airoha,
> and I noticed that the three ambient `modes` — ANC, Passthrough, and OFF — `can` be toggled `within` the app.
> There's also an activity `that lets you` reassign functions to each button action.
> 
> Even the sample App supports those `features` and the SDK `is available on` MediaTek Online.
> So I don't think what you mentioned `would be` that difficult.
> 
> So I'd say what you mentioned should be doable.
>
> > [情報追加してみた]
> > I've been tinkering `with` the Hear Better App, a sample app released by Airoha,
> > and I noticed that the three ambient `modes` — ANC, Passthrough, and OFF — `can` be toggled `within` the app.
> > There's also an activity `that lets you` reassign functions to each button action.
> >
> > Even the sample App supports those things and you can get the SDK of this App from MediaTek Online.
> > So I think what you mentioned isn't, that difficult.
> >
> > So I'd say what you mentioned should be doable.
> >
> > > [ChatGPT]
> > > I've been tinkering `with` the Hear Better App, a sample app released by Airoha,
> > > and I noticed that the three ambient `modes` — ANC, Passthrough, and OFF — `can` be toggled `within` the app.
> > > There's also an activity `that lets you` reassign functions to each button action.
> > > So I'd say what you mentioned should be doable.
> > > 
> > > > [情報追加してみた]
> > > > I've been tinkering the Hear Better App which is a sample app released by Airoha,
> > > > and I noticed that these three ambient status, ANC, Passthrough, and OFF, could be toggled in the app.
> > > > There's `also` an activity `that allows you to` reassign functions to each button action.
> > > > So, I’d say what you mentioned `should be doable`.
> > > >
> > > > > これだと、背景が足りない、パラメータってなんだ？、、、
> > > > >
> > > > > [ChatGPT]
> > > > > I `noticed` three parameters `being toggled` `in` the Hear Better App, which is a sample app released `by` Airoha.
> > > > > There's `also` an activity `that allows you to` reassign functions to each button action.
> > > > > So, I’d say what you mentioned `should be doable`.
> > > > >
> > > > > > [原文]
> > > > > > I did see three parameters toggling on Hear Better App which is a sample app released from Airoha.
> > > > > > And there is the activity for reassigning functionalities to each button action.
> > > > > > So I'd say we can do what you said.
